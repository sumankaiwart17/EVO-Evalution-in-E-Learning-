$('#switchLang').on('click', function (id) { 

    alert(id);

    // foreach (Config::get('languages') as $lang => $language)
    //         if ($lang != App::getLocale())     
           
    // $.ajax({
    //     type: 'GET',
    //     url: 'lang.switch' + $lang,
    // })
});